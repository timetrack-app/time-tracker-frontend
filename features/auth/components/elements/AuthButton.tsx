import styled from 'styled-components';
import { ButtonPrimary } from '../../../../components/elements/common';

const AuthButton = styled(ButtonPrimary)`
  width: 30%;
`;

export default AuthButton;
